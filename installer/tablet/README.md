# Instalación de tablet

Usa `Install-POSFlutterTablet.ps1` con un APK firmado y su SHA256 verificado. El script no borra datos; `Reset-POSFlutterTablet.ps1` requiere confirmación explícita y sólo se usa para UAT limpio autorizado.
