import 'package:flutter/material.dart';import 'package:pos_app/core/app_services.dart';import 'package:pos_app/features/sales/data/sales_repository.dart';import 'package:pos_app/shared/presentation/database_list_screen.dart';class SalesScreen extends StatelessWidget{const SalesScreen({super.key});@override Widget build(BuildContext context)=>DatabaseListScreen(title:'Ventas',query:'SELECT id,folio,total_cents,status,global_id FROM sales ORDER BY id DESC',action:(c)async{final v=await textForm(c,'Cancelar venta',['Sale Global ID','Motivo']);if(v!=null)await SalesRepository(appDatabase).cancel(v[0],v[1]);});}
