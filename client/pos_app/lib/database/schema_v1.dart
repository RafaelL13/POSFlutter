const List<String> schemaV1Statements = [
  r'''CREATE TABLE businesses(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,name TEXT NOT NULL,active INTEGER NOT NULL DEFAULT 1 CHECK(active IN(0,1)),created_at TEXT NOT NULL,updated_at TEXT NOT NULL)''',
  r'''CREATE TABLE branches(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,business_id INTEGER NOT NULL REFERENCES businesses(id),name TEXT NOT NULL,active INTEGER NOT NULL DEFAULT 1 CHECK(active IN(0,1)),created_at TEXT NOT NULL,updated_at TEXT NOT NULL)''',
  r'''CREATE TABLE devices(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,branch_id INTEGER NOT NULL REFERENCES branches(id),name TEXT NOT NULL,active INTEGER NOT NULL DEFAULT 1 CHECK(active IN(0,1)),created_at TEXT NOT NULL,updated_at TEXT,last_sync_at TEXT)''',
  r'''CREATE TABLE users(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,business_id INTEGER NOT NULL REFERENCES businesses(id),name TEXT NOT NULL,username TEXT NOT NULL,password_hash TEXT NOT NULL,password_salt TEXT NOT NULL,role TEXT NOT NULL CHECK(role IN('Administrator','Seller','Supervisor','Manager')),active INTEGER NOT NULL DEFAULT 1 CHECK(active IN(0,1)),created_at TEXT NOT NULL,updated_at TEXT NOT NULL,UNIQUE(business_id,username))''',
  r'''CREATE TABLE categories(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,business_id INTEGER NOT NULL REFERENCES businesses(id),name TEXT NOT NULL,description TEXT,active INTEGER NOT NULL DEFAULT 1 CHECK(active IN(0,1)),created_at TEXT NOT NULL,updated_at TEXT NOT NULL,UNIQUE(business_id,name))''',
  r'''CREATE TABLE suppliers(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,business_id INTEGER NOT NULL REFERENCES businesses(id),name TEXT NOT NULL,contact_name TEXT,phone TEXT,email TEXT,address TEXT,notes TEXT,active INTEGER NOT NULL DEFAULT 1 CHECK(active IN(0,1)),created_at TEXT NOT NULL,updated_at TEXT NOT NULL)''',
  r'''CREATE TABLE products(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,business_id INTEGER NOT NULL REFERENCES businesses(id),code TEXT NOT NULL,barcode TEXT,name TEXT NOT NULL,description TEXT,category_id INTEGER REFERENCES categories(id),presentation TEXT NOT NULL DEFAULT 'Piece',sale_price_cents INTEGER NOT NULL CHECK(typeof(sale_price_cents)='integer' AND sale_price_cents>=0),minimum_stock INTEGER NOT NULL DEFAULT 0 CHECK(typeof(minimum_stock)='integer' AND minimum_stock>=0),active INTEGER NOT NULL DEFAULT 1 CHECK(active IN(0,1)),created_at TEXT NOT NULL,updated_at TEXT NOT NULL,deleted_at TEXT,UNIQUE(business_id,code))''',
  r'''CREATE TABLE purchases(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,supplier_id INTEGER NOT NULL REFERENCES suppliers(id),branch_id INTEGER NOT NULL REFERENCES branches(id),device_id INTEGER NOT NULL REFERENCES devices(id),user_id INTEGER NOT NULL REFERENCES users(id),purchase_date TEXT NOT NULL,reference TEXT,notes TEXT,total_cents INTEGER NOT NULL CHECK(typeof(total_cents)='integer' AND total_cents>=0),status TEXT NOT NULL DEFAULT 'Confirmed',created_at TEXT NOT NULL)''',
  r'''CREATE TABLE purchase_details(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,purchase_id INTEGER NOT NULL REFERENCES purchases(id) ON DELETE CASCADE,product_id INTEGER NOT NULL REFERENCES products(id),quantity INTEGER NOT NULL CHECK(typeof(quantity)='integer' AND quantity>0),unit_cost_cents INTEGER NOT NULL CHECK(typeof(unit_cost_cents)='integer' AND unit_cost_cents>=0),subtotal_cents INTEGER NOT NULL CHECK(typeof(subtotal_cents)='integer' AND subtotal_cents>=0))''',
  r'''CREATE TABLE inventory_lots(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,product_id INTEGER NOT NULL REFERENCES products(id),purchase_detail_id INTEGER REFERENCES purchase_details(id),branch_id INTEGER NOT NULL REFERENCES branches(id),entry_date TEXT NOT NULL,initial_quantity INTEGER NOT NULL CHECK(typeof(initial_quantity)='integer' AND initial_quantity>0),available_quantity INTEGER NOT NULL CHECK(typeof(available_quantity)='integer' AND available_quantity>=0),unit_cost_cents INTEGER NOT NULL CHECK(typeof(unit_cost_cents)='integer' AND unit_cost_cents>=0),active INTEGER NOT NULL DEFAULT 1 CHECK(active IN(0,1)),created_at TEXT NOT NULL)''',
  r'''CREATE TABLE inventory_movements(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,product_id INTEGER NOT NULL REFERENCES products(id),branch_id INTEGER NOT NULL REFERENCES branches(id),movement_date TEXT NOT NULL,type TEXT NOT NULL,quantity_delta INTEGER NOT NULL CHECK(typeof(quantity_delta)='integer'),previous_stock INTEGER NOT NULL CHECK(typeof(previous_stock)='integer'),new_stock INTEGER NOT NULL CHECK(typeof(new_stock)='integer'),reference_global_id TEXT NOT NULL,user_id INTEGER NOT NULL REFERENCES users(id),device_id INTEGER NOT NULL REFERENCES devices(id),notes TEXT)''',
  r'''CREATE TABLE sales(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,idempotency_key TEXT NOT NULL UNIQUE,folio TEXT NOT NULL,sale_datetime TEXT NOT NULL,user_id INTEGER NOT NULL REFERENCES users(id),device_id INTEGER NOT NULL REFERENCES devices(id),branch_id INTEGER NOT NULL REFERENCES branches(id),subtotal_cents INTEGER NOT NULL CHECK(typeof(subtotal_cents)='integer' AND subtotal_cents>=0),discount_cents INTEGER NOT NULL DEFAULT 0 CHECK(typeof(discount_cents)='integer' AND discount_cents>=0),total_cents INTEGER NOT NULL CHECK(typeof(total_cents)='integer' AND total_cents>=0),fifo_cost_cents INTEGER NOT NULL CHECK(typeof(fifo_cost_cents)='integer' AND fifo_cost_cents>=0),gross_profit_cents INTEGER NOT NULL CHECK(typeof(gross_profit_cents)='integer'),payment_method TEXT NOT NULL,received_cents INTEGER,change_cents INTEGER NOT NULL DEFAULT 0 CHECK(typeof(change_cents)='integer' AND change_cents>=0),status TEXT NOT NULL DEFAULT 'Confirmed',created_at TEXT NOT NULL,updated_at TEXT NOT NULL,cancelled_at TEXT,cancellation_reason TEXT,cancelled_by_user_id INTEGER REFERENCES users(id))''',
  r'''CREATE TABLE sale_details(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,sale_id INTEGER NOT NULL REFERENCES sales(id) ON DELETE CASCADE,product_id INTEGER NOT NULL REFERENCES products(id),quantity INTEGER NOT NULL CHECK(typeof(quantity)='integer' AND quantity>0),unit_price_cents INTEGER NOT NULL CHECK(typeof(unit_price_cents)='integer' AND unit_price_cents>=0),discount_cents INTEGER NOT NULL DEFAULT 0 CHECK(typeof(discount_cents)='integer' AND discount_cents>=0),total_cents INTEGER NOT NULL CHECK(typeof(total_cents)='integer' AND total_cents>=0),fifo_cost_cents INTEGER NOT NULL CHECK(typeof(fifo_cost_cents)='integer' AND fifo_cost_cents>=0))''',
  r'''CREATE TABLE sale_detail_lots(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,sale_detail_id INTEGER NOT NULL REFERENCES sale_details(id) ON DELETE CASCADE,inventory_lot_id INTEGER NOT NULL REFERENCES inventory_lots(id),quantity INTEGER NOT NULL CHECK(typeof(quantity)='integer' AND quantity>0),unit_cost_cents INTEGER NOT NULL CHECK(typeof(unit_cost_cents)='integer' AND unit_cost_cents>=0),total_cost_cents INTEGER NOT NULL CHECK(typeof(total_cost_cents)='integer' AND total_cost_cents>=0))''',
  r'''CREATE TABLE cash_sessions(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,branch_id INTEGER NOT NULL REFERENCES branches(id),device_id INTEGER NOT NULL REFERENCES devices(id),user_id INTEGER NOT NULL REFERENCES users(id),opened_at TEXT NOT NULL,opening_balance_cents INTEGER NOT NULL CHECK(typeof(opening_balance_cents)='integer' AND opening_balance_cents>=0),status TEXT NOT NULL DEFAULT 'Open',closed_at TEXT,counted_cash_cents INTEGER,expected_cash_cents INTEGER,difference_cents INTEGER,updated_at TEXT NOT NULL)''',
  r'''CREATE TABLE cash_movements(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,cash_session_id INTEGER NOT NULL REFERENCES cash_sessions(id),movement_date TEXT NOT NULL,type TEXT NOT NULL,amount_cents INTEGER NOT NULL CHECK(typeof(amount_cents)='integer'),reference_global_id TEXT,user_id INTEGER NOT NULL REFERENCES users(id),notes TEXT)''',
  r'''CREATE TABLE expenses(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,expense_date TEXT NOT NULL,concept TEXT NOT NULL,category TEXT,amount_cents INTEGER NOT NULL CHECK(typeof(amount_cents)='integer' AND amount_cents>0),payment_method TEXT NOT NULL,notes TEXT,user_id INTEGER NOT NULL REFERENCES users(id),device_id INTEGER NOT NULL REFERENCES devices(id),branch_id INTEGER NOT NULL REFERENCES branches(id),created_at TEXT NOT NULL)''',
  r'''CREATE TABLE audit_logs(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,entity_type TEXT NOT NULL,entity_global_id TEXT NOT NULL,action TEXT NOT NULL,user_id INTEGER REFERENCES users(id),device_id INTEGER REFERENCES devices(id),created_at TEXT NOT NULL,details_json TEXT NOT NULL DEFAULT '{}')''',
  r'''CREATE TABLE sync_queue(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,entity_type TEXT NOT NULL,entity_global_id TEXT NOT NULL,operation TEXT NOT NULL,payload_version INTEGER NOT NULL DEFAULT 1,payload_json TEXT NOT NULL,created_at TEXT NOT NULL,last_attempt_at TEXT,retry_count INTEGER NOT NULL DEFAULT 0,status TEXT NOT NULL DEFAULT 'Pending' CHECK(status IN('Pending','Syncing','Synced','Error')),error_message TEXT,next_attempt_at TEXT)''',
  r'''CREATE TABLE app_settings(key TEXT PRIMARY KEY,value TEXT NOT NULL,updated_at TEXT NOT NULL)''',
  r'''CREATE TABLE inventory_adjustments(id INTEGER PRIMARY KEY AUTOINCREMENT,global_id TEXT NOT NULL UNIQUE,product_id INTEGER NOT NULL REFERENCES products(id),branch_id INTEGER NOT NULL REFERENCES branches(id),quantity_delta INTEGER NOT NULL CHECK(typeof(quantity_delta)='integer' AND quantity_delta<>0),reason TEXT NOT NULL,user_id INTEGER NOT NULL REFERENCES users(id),device_id INTEGER NOT NULL REFERENCES devices(id),created_at TEXT NOT NULL)''',
  r'''CREATE INDEX ix_inventory_lots_fifo ON inventory_lots(product_id,branch_id,entry_date,id)''',
  r'''CREATE INDEX ix_inventory_movements_product_date ON inventory_movements(product_id,branch_id,movement_date)''',
  r'''CREATE INDEX ix_sync_queue_status_id ON sync_queue(status,id)''',
];
