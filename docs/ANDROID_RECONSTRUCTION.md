# Android host reconstruction

Status: **SOURCE/CONFIG RECONSTRUCTED — WRAPPER BINARIES PENDING — BUILD NOT EXECUTED**.

The Android host was manually reconstructed during controlled reconstruction. It was **not** generated by Flutter in this runtime.

## Reconstructed configuration

- Gradle distribution declared in `gradle/wrapper/gradle-wrapper.properties`: `9.3.1`
- Android Gradle Plugin: `9.1.0`
- Kotlin Android plugin: `2.4.0`
- Java source/target compatibility: `17`
- Kotlin JVM target: `17`
- `compileSdk`: `36`
- `targetSdk`: `36`
- `minSdk`: `24`
- namespace/applicationId: `com.posflutter.pos_app`
- `MainActivity`: `com.posflutter.pos_app.MainActivity`
- cleartext HTTP disabled in the main manifest

The source/configuration values are internally consistent at the text/configuration level. They have **not** been validated by Gradle or Flutter because those tools are absent from the runtime.

## Intentionally missing standard wrapper artifacts

The following files were not fabricated:

- `android/gradlew`
- `android/gradlew.bat`
- `android/gradle/wrapper/gradle-wrapper.jar`

The runtime has no Flutter SDK, Gradle installation, or trusted network path from which to obtain the official wrapper artifacts. Before the first Android build, regenerate or copy these files from an official Gradle/Flutter source and compare the Android host with a fresh project using the intended Flutter release.

`android/local.properties` is also intentionally not versioned; Flutter/Android tooling must create it locally with the real SDK paths.

## Signing

No production keystore or credentials are stored in this repository. The reconstructed `release` block currently uses the debug signing configuration only as a non-production scaffold. Before distribution, configure a private release keystore outside Git and update the local signing setup.
