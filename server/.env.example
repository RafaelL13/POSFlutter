# Docker Compose development values. Replace outside Git.
MSSQL_SA_PASSWORD=CHANGE_ME_STRONG_PASSWORD
JWT_SIGNING_KEY=CHANGE_ME_AT_LEAST_32_RANDOM_BYTES

# API/IIS environment variable names. Do not commit real values.
# ConnectionStrings__SqlServer=Server=localhost;Database=POSFlutter;Integrated Security=True;TrustServerCertificate=True
# Jwt__SigningKey=replace-with-a-random-secret-managed-outside-git
